[project]
name = "nfl-sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "sqlalchemy",
    "psycopg2-binary",
    "ruff",
    "pytest",
    "ipykernel",
]
